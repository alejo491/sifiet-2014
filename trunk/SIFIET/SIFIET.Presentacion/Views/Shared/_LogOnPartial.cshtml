@if (Request.IsAuthenticated)
{
    <text>Hola <strong>@Context.User.Identity.Name</strong>!
    [ @Html.ActionLink("Cerrar Sesión", "LogOut", "Usuarios") ]</text>
}
else
{
    @:[ @Html.ActionLink("Iniciar Sesión", "LogOn", "Usuarios") ]
}
